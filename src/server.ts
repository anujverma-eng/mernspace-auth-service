function login(name: string): string {
  // console.log(name);

  return name;
}

login("Anuj");

const foo = () => 0;

foo();

const bar = () => {
  return {
    bar: {
      foo: 1,
      bar: 2,
    },
  };
};

bar();
